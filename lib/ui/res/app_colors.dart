import 'package:flutter/material.dart';

/// цвета для расширения темы
class AppColors {
  static const white = Colors.white;

  /// цвета для градиентной заливки фона экранов
  static const bgScreenOneLight = Color(0xFF88D0DE);
  static const bgScreenOneDark = Color(0xFF29A7E4);

  static const bgScreenTwoLight = Color(0xFF4AB6AD);
  static const bgScreenTwoDark = Color(0xFF066C64);

  /// нет ошибок в ответах
  static const bgScreenThreeLight = Color(0xFF066C64);
  static const bgScreenThreeDark = Color(0xFF29A7E4);

  /// есть хотя бы 1 ошибка
  static const bgScreenFourLight = Color(0xFFFFAC12);
  static const bgScreenFourDark = Color(0xFFFF7415);

  /// для кнопок
  static const bgButton = Color(0xFF0D74B2);
  static const splashButton = Color(0xFF90CED8);

  /// для кнопки ответа
  static const bgButtonResponse = Color(0xFFF9F9F9);
  static const splashButtonResponse = Color(0xFF066C64);
  static const titleButtonResponse = Color(0xFF656565);
  static const dividerButtonResponse = Color(0xFFE7E6E6);
  static const colorShadow = Color(0x54000000);

  /// цвета индикатора
  static const colorIndicatorLight = Color(0xFFF9F9F9);
  static const colorIndicatorDark = Color(0xFFFFAC12);

  AppColors._();
}
